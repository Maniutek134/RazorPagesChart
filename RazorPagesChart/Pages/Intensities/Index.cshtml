@page

@model RazorPagesChart.Pages.Intensities.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Intensity[0].RoadSegmentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Intensity[0].Intenstiy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Intensity[0].MeasureTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Intensity[0].DbTemperature)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Intensity) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoadSegmentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Intenstiy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MeasureTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DbTemperature.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
